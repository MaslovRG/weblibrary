@model X.PagedList.PagedList<Gateway.Models.Books.Book>
@using X.PagedList.Mvc.Core;
@{
    ViewBag.Title = "Books";
}

<h2>Books</h2>

<table class="table">
    <tr>
        <th>Title</th>
        <th>Year</th>
        <th>Author</th>
    </tr>

    @if (Model != null)
    {
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <a asp-action="Book" asp-route-name="@item.Name">@item.Name</a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Year)
                </td>
                <td>
                    <a asp-action="Author" asp-controller="FEAuthor" asp-route-name="@item.Author">@item.Author</a>
                </td>
            </tr>
        }
    }
    else
    {
        <tr>
            <td>Books not found</td>
            <td></td>
            <td></td>
        </tr>
    }
        <tr>
            <td><a asp-action="AddBook">Add new book</a></td>
            <td></td>
            <td></td>
        </tr>
</table>
<br />
@if (Model != null)
{
    <span>
        Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount

        @Html.PagedListPager(Model, page => Url.Action("Books", new { page }))
    </span>
}
